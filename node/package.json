{
  "name": "workflowy-calendar-generator",
  "version": "0.0.1",
  "description": "Generate a simple HTML list for the given where each list item is a month, which in itself is a list where each list item is a week, which in itself is a list where each list item is a day",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "debug": "nodemon --inspect index.js",
    "update:packages": "node wipe-dependencies.js && rm -rf node_modules && npm update --save-dev && npm update --save"
  },
  "keywords": ["workflowy"],
  "author": "Harm te Molder",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "body-parser": "*",
    "express": "*",
    "handlebars": "*",
    "helmet-csp": "*",
    "html-entities": "*",
    "moment": "*",
    "node-sass-middleware": "*"
  },
  "devDependencies": {
    "eslint": "*",
    "eslint-config-airbnb": "*",
    "eslint-plugin-import": "*",
    "eslint-plugin-jsx-a11y": "*",
    "eslint-plugin-react": "*",
    "nodemon": "*"
  }
}
